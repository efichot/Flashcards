extends layout

block content
		section#content
		if name
            h2 Welcome, #{name}!
            form(action='/goodbye', method='get')
                button(type='submit', name='goodbye') Goodbye!
			p
				a(href='/cards') Begin!
